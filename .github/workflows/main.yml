name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: go test
      run: make go-test
    - name: go lint
      run: make go-lint
